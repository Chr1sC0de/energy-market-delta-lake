interface:
  display_name: "Shape Issues"
  short_description: "Shape and gate Ralph issue drafts"
  default_prompt: "Use $shape-issues to turn this plan into tracer-bullet issue drafts with context anchors, stiffness scoring, and optional confirmed publication as needs-triage issues."
